name: td_display
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.3.1 <4.0.0'

dependencies:
  syncfusion_flutter_charts: ^25.1.41+2
  fluent_ui: ^4.8.7
  tt_bindings:
    path: ../tt_bindings

  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
